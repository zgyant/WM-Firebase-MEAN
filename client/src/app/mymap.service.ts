import { Injectable } from '@angular/core';
import { Mymapclass } from './mymapclass';
import {Observable} from 'rxjs';
import {HttpClient, HttpInterceptor, HttpHandler, HttpEvent, HttpHeaders, HTTP_INTERCEPTORS, HttpRequest, HttpResponse } from '@angular/common/http';

@Injectable({
  providedIn: 'root'
})
export class MymapService {

  constructor() { }
 
  
}
